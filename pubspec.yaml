name: assignment
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.5.0 <4.0.0"  # Updated to follow proper range constraints

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  http: ^1.1.0  # Moved from dev_dependencies
  shared_preferences: ^2.1.1  # Moved from dev_dependencies
  loading_overlay: ^0.4.2  # Package for showing loading overlays
  image_picker: ^0.8.6
  fluttertoast: ^8.2.1


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
